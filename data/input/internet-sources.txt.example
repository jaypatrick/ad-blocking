# Example internet sources file
# Add one URL per line pointing to remote filter lists
# Lines starting with # are comments

# SECURITY REQUIREMENTS:
# - Only HTTPS URLs are allowed (HTTP is rejected)
# - URLs are validated before download
# - Content is scanned to verify it contains valid filter rules
# - Optionally add #sha384=<hash> to verify integrity

# Popular filter lists (trusted sources)
https://easylist.to/easylist/easylist.txt
https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts

# AdGuard filter lists (trusted sources)
# https://filters.adtidy.org/extension/chromium/filters/2.txt

# With hash verification for maximum security
# https://easylist.to/easylist/easylist.txt#sha384=abc123def456...

# Custom or organizational lists
# https://your-organization.com/custom-blocklist.txt

# IMPORTANT SECURITY NOTES:
# 1. Only add URLs from trusted sources
# 2. Verify the domain legitimacy before adding
# 3. Review downloaded content before production use
# 4. Use hash verification for critical sources
# 5. Monitor for unexpected changes in list size/content
