#
# AdGuard DNS API
# DNS API documentation
# Version: 1.11
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

DNS protocol type

.PARAMETER Json

JSON object

.OUTPUTS

DnsProtoType<PSCustomObject>
#>
function ConvertFrom-JsonToDnsProtoType {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match RegularDnsProtoType defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToRegularDnsProtoType $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "RegularDnsProtoType"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'RegularDnsProtoType' defined in oneOf (DnsProtoType). Proceeding to the next one if any."
        }

        # try to match SecureDnsProtoType defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToSecureDnsProtoType $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SecureDnsProtoType"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SecureDnsProtoType' defined in oneOf (DnsProtoType). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([RegularDnsProtoType, SecureDnsProtoType]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("RegularDnsProtoType", "SecureDnsProtoType")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([RegularDnsProtoType, SecureDnsProtoType]). JSON Payload: $($Json)"
        }
    }
}

