# Bun configuration for ad-blocking-website (Gatsby)
# https://bun.sh/docs/runtime/bunfig
#
# Note: Gatsby has experimental Bun support. Use `bun install` for faster
# dependency installation, but run Gatsby commands through npm/npx for stability.

[install]
# Use exact versions for reproducible builds
exact = true

# Gatsby may have issues with some hoisting
[install.scopes]
# Add any scope-specific configurations here if needed
